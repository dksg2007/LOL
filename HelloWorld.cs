using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Le dice Hola a Dylan
        print("Hola, Dylan");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
